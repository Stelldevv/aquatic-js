{
  "name": "aquatic-js",
  "version": "1.0.0",
  "description": "A simple, randomized environment for people who like staring at fish. I'm not very good at this.",
  "scripts": {
    "dev": "concurrently -k \"tsc -p ./src/server -w\" \"tsc -p ./src/client -w\" \"nodemon ./dist/server/server.js\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon ./dist/server/server.js"
  },
  "author": "Joshua Stelly",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.8",
    "@types/node": "^14.11.2",
    "express": "^4.17.1",
    "three": "^0.120.1"
  },
  "devDependencies": {
    "@types/stats": "^0.16.30",
    "@types/stats.js": "^0.17.0",
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.4"
  }
}
